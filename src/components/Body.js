import NavigationBar from "./NavigationBar";
import Grid from "@material-ui/core/Grid";

function Body() {
  return (
    <Grid container>
      <NavigationBar />
      <div>Konten</div>
    </Grid>
  );
}

export default Body;
